import * as userTypes from '../types/user';

const USER_LOGIN = () => {
  return {
    type: userTypes.USER_LOGIN,
    payload: {},
  };
};

const USER_SIGNUP = () => {
  return {
    type: userTypes.USER_SIGNUP,
    payload: {},
  };
};
